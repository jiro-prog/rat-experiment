\documentclass{article}

\usepackage[preprint]{neurips_2024}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{float}
\usepackage{multirow}

\title{Zero-Shot Embedding Space Translation\\via Relative Anchor Similarity Profiles}

\author{%
  Sojiro Uemoto
}

\begin{document}

\maketitle

\begin{abstract}
Different embedding models map inputs to incompatible vector spaces, forcing costly re-embedding when models are switched or combined. We present \textbf{Relative Anchor Translation (RAT)}, a zero-shot protocol that translates between embedding spaces by comparing similarity profiles to shared anchor points, requiring no training data or learned parameters.

Evaluating 17 models from 9 families---spanning BERT-family encoders, a decoder-based LLM, and a scale series from 22M to 335M parameters---we identify a \textbf{three-layer hierarchy} governing translation accuracy, with \textbf{RDM correlation as a binary gatekeeper}. BERT-family and newer models share zero representational structure at every level (global distances, local neighborhoods, cluster assignments), creating a hard boundary that no method---zero-shot or few-shot, parameter-free or learned---can cross. Scaling parameters 15$\times$ within a training recipe does not bridge this gap. Within compatible clusters, \textbf{similarity compression} ($\rho = -0.62$) predicts accuracy and guides method selection. RAT achieves Recall@1 from 13\% to 99\% across 110 directed pairs within compatible families, with a crossover against linear alignment at $K \approx 100$ anchors. We provide a practical decision protocol computable from anchor embeddings alone: determine feasibility, select the optimal method, and predict accuracy---without retrieval evaluation. Code: \texttt{pip install rat-embed}.
\end{abstract}

%% ============================================================
\section{Introduction}
\label{sec:intro}
%% ============================================================

The proliferation of embedding models presents a growing interoperability problem. Organizations accumulate vector databases indexed by one model, only to find that newer or specialized models produce incompatible representations. Switching models requires re-embedding entire corpora---a cost that scales linearly with database size and becomes prohibitive for large-scale systems. Multi-model deployments, where different components use different encoders, face the same barrier: vectors from one model cannot be directly compared with vectors from another.

Existing approaches to embedding space alignment rely on learning a transformation between spaces. Procrustes alignment \citep{conneau2018word} fits an orthogonal mapping from parallel data; knowledge distillation \citep{hinton2015distilling} trains a student to mimic a teacher's outputs. These methods require paired training data, model-specific optimization, and separate transforms for each model pair---costs that grow quadratically with the number of models in a system.

We propose \textbf{Relative Anchor Translation (RAT)}, a zero-shot protocol that translates between arbitrary embedding spaces without any training. The core idea is simple: instead of comparing vectors directly, we compare their \emph{similarity profiles}---vectors of similarities to a shared set of anchor points. When two models encode the same text, their patterns of similarity to common reference concepts tend to align, even though their absolute coordinates do not. This transforms the problem from aligning high-dimensional spaces of different dimensions to comparing response patterns in a shared, low-dimensional anchor space.

RAT builds on the relative representation framework of \citet{moschella2023relative}, who showed that cosine similarities to random anchors can enable zero-shot model stitching. We extend this framework in three ways that collectively raise performance from proof-of-concept to practical levels:
\begin{itemize}
  \item \textbf{Farthest Point Sampling} for anchor selection eliminates the density bias of random anchors, achieving with 100 anchors what random selection requires 1,000 to match (\S\ref{sec:scaling}).
  \item \textbf{Polynomial kernel} similarity $\kappa(u,v) = (u^\top v + 1)^2$ amplifies discriminability at the top of the ranked list, adding +11 points over cosine similarity.
  \item \textbf{DB-side z-score normalization} resolves a previously uncharacterized failure mode---\textbf{similarity collapse}---where compressed similarity distributions in certain model spaces (e.g., multilingual encoders) flatten relative representations and destroy discriminability. This single correction adds +65 points on the most affected pair (\S\ref{sec:collapse}).
\end{itemize}

We evaluate RAT on 17 text embedding models from 9 families, spanning architectures (BERT, XLM-R, CLIP, MPNet, decoder-based LLM), sizes (22M--1.5B), and training objectives (contrastive, distillation, weakly-supervised, instruction-tuned). Among 110 directed pairs within compatible families, Recall@1 ranges from 13\% to 99\%. We further extend RAT across modalities using \textbf{Rosetta Stone anchors}---paired (image, caption) concepts that allow a text-only encoder to search a vision encoder's space at Recall@1 = 21.2\%, with zero visual training.

Four main findings emerge from the 17-model evaluation:
\begin{enumerate}
  \item \textbf{A three-layer diagnostic model for embedding translation.} We identify a hierarchy of factors: (1)~RDM correlation acts as a binary gatekeeper determining whether translation is feasible; (2)~similarity compression ($\rho = -0.62$) governs accuracy within compatible families; (3)~family membership and dimensionality refine predictions. This yields a practical decision protocol computable from anchor embeddings alone (\S\ref{sec:diagnostic}).
  \item \textbf{The cross-cluster wall is absolute.} Extending to 17 models reveals a hard representational boundary: BERT-family and new-generation models share zero structure at every level---global (RDM $\rho \approx 0$), local ($k$-NN overlap $=$ random), and cluster (ARI $\approx 0$). Neither zero-shot methods nor few-shot linear maps (up to 200 labeled pairs) can cross it. Scaling parameters 15$\times$ within a training recipe produces no movement (\S\ref{sec:wall}).
  \item \textbf{Similarity compression dominates within-cluster accuracy.} Z-score normalization adds up to +88 points on low-compression databases but destroys accuracy ($-$62 points) on compressed ones, diagnosable from sim\_mean alone (\S\ref{sec:asymmetric}).
  \item \textbf{RAT and direct alignment are complementary.} A comparison across 132 pairs reveals a crossover at $K \approx 100$ anchors: RAT dominates with fewer anchors (74\% win rate at $K{=}50$); linear methods dominate with more ($>$90\% at $K{=}500$). Within clusters, few-shot Procrustes further improves on RAT (87\% vs.\ 79\% with 200 labeled pairs; \S\ref{sec:alignment}, \S\ref{sec:wall_positive}).
\end{enumerate}

%% ============================================================
\section{Method}
\label{sec:method}
%% ============================================================

\subsection{Relative Anchor Representation}

Let $f_X: \mathcal{T} \to \mathbb{R}^{d_X}$ and $f_Y: \mathcal{T} \to \mathbb{R}^{d_Y}$ be two embedding models mapping inputs to L2-normalized vectors, where $d_X \neq d_Y$ in general. Given a shared set of $K$ anchor texts $\mathcal{A} = \{a_1, \dots, a_K\}$, we define the \emph{relative anchor representation} of an input $x$ under model $X$ as:
\begin{equation}
  r_X(x) = \left[ \kappa(f_X(x), f_X(a_1)), \dots, \kappa(f_X(x), f_X(a_K)) \right] \in \mathbb{R}^K
  \label{eq:relative}
\end{equation}
where $\kappa$ is a kernel function. This transforms vectors from model-specific $d_X$-dimensional spaces into a shared $K$-dimensional space independent of the original dimensions.

We use the polynomial kernel $\kappa(u, v) = (u^\top v + 1)^2$, which outperforms cosine similarity by +11 percentage points on Recall@1 (Table~\ref{tab:kernels} in Appendix). The quadratic nonlinearity amplifies differences among the highest-similarity anchors, improving discrimination at the top of the ranked list.

\subsection{Anchor Selection via Farthest Point Sampling}

Random anchor selection suffers from \emph{density bias}: anchors cluster in high-density regions. We apply Farthest Point Sampling (FPS) to maximize spatial coverage: starting from a random seed, each subsequent anchor is the candidate \emph{most distant} from all previously selected anchors (measured by cosine similarity in a single model's space). This greedy $O(KN)$ procedure runs in seconds on CPU. FPS is executed in Model~A's space; the selected indices are shared across all models.

\subsection{DB-side z-score Normalization}

When anchor inter-similarity is compressed into a narrow range (e.g., E5-large: mean pairwise similarity = 0.72, range = 0.33), relative representations become \emph{flat}, destroying discriminability (\S\ref{sec:collapse}). We apply z-score normalization \textbf{exclusively to the database side}:
\begin{equation}
  \hat{r}_Y(y_j) = \frac{r_Y(y_j) - \mu_j}{\sigma_j}, \quad \mu_j = \frac{1}{K}\sum_k [r_Y(y_j)]_k, \quad \sigma_j = \mathrm{std}(r_Y(y_j))
\end{equation}
The query representation $r_X(x)$ is left unnormalized. DB-side z-score exactly matches both-side normalization across all pairs, while query-side normalization is consistently harmful (\S\ref{sec:asymmetric}).

\textbf{Adaptive application.} Z-score is conditionally beneficial: it helps when the DB model has low similarity compression but \emph{destroys} accuracy on compressed models (\S\ref{sec:asymmetric}). We apply z-score adaptively based on the DB model's sim\_mean (mean pairwise cosine similarity among anchors): apply when sim\_mean $< 0.65$, skip otherwise. This threshold is diagnosable from anchor embeddings alone, requiring no retrieval evaluation.

Algorithm~1 summarizes the complete protocol.

\begin{figure}[t]
\centering
\small
\fbox{\parbox{0.92\columnwidth}{%
\textbf{Algorithm 1:} Relative Anchor Translation\\[3pt]
\textbf{Input:} Query $x$ via model $X$; database $\{y_1,\dots,y_n\}$ via model $Y$; candidates $\mathcal{C}$\\
\textbf{Assumption:} All embeddings are L2-normalized (so $u^\top v = \cos(u,v)$).\\[4pt]
\textbf{Offline} (once per model pair):\\
\quad 1. Encode candidates: $E_X = f_X(\mathcal{C})$\\
\quad 2. Select $K$ anchors via FPS on $E_X$: $\mathcal{A} = \mathrm{FPS}(E_X, K)$\\
\quad 3. Encode anchors: $A_X = f_X(\mathcal{A})$,\; $A_Y = f_Y(\mathcal{A})$\\
\quad 4. Compute DB relative repr.: $R_Y[j,k] = (f_Y(y_j)^\top A_Y[k] + 1)^2$\\
\quad 5. Apply z-score to $R_Y$ (row-wise) $\to \hat{R}_Y$\\[4pt]
\textbf{Online} (per query):\\
\quad 6. $r_X(x) = [(f_X(x)^\top A_X[k] + 1)^2]_{k=1}^K$\\
\quad 7. Retrieve: $\hat{j} = \arg\max_j \cos(r_X(x),\, \hat{R}_Y[j])$
}}
\label{alg:rat}
\end{figure}

%% ============================================================
\section{Cross-Modal Extension: Rosetta Stone Anchors}
\label{sec:rosetta}
%% ============================================================

For cross-modal retrieval between a text encoder $f_T$ and an image encoder $f_I$, we introduce \textbf{Rosetta Stone anchors}: $K$ concept-aligned pairs $\{(t_k, i_k)\}_{k=1}^K$ where $t_k$ is a caption and $i_k$ the corresponding image (Figure~\ref{fig:pipeline}b). Each model encodes its own modality: $r_T(x) = [\kappa(f_T(x), f_T(t_k))]_{k=1}^K$ and $r_I(v) = [\kappa(f_I(v), f_I(i_k))]_{k=1}^K$. Both representations encode the same concept's \emph{response pattern} through different modalities. We use image-caption pairs from COCO Karpathy \citep{lin2014microsoft,karpathy2015deep}, with FPS in the text space.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.75\textwidth]{figures/figure1_rat_pipeline.png}
  \caption{\textbf{RAT pipeline.} (a)~Text$\times$Text: both models encode the same anchor texts; relative representations live in a shared $K$-dimensional space regardless of original dimensions. (b)~Cross-modal: Rosetta Stone anchors provide concept-aligned (caption, image) pairs; each model encodes its own modality.}
  \label{fig:pipeline}
\end{figure}

%% ============================================================
\section{Experiments}
\label{sec:experiments}
%% ============================================================

\subsection{Setup}

\textbf{Models.} Initial experiments (\S\ref{sec:text}--\S\ref{sec:crossmodal}) use five text encoders: MiniLM (384d, A), E5-large (1024d, multilingual, B), BGE-small (384d, C), CLIP-text (512d, D), BGE-large (1024d, E); plus CLIP ViT-B/32 image encoder for cross-modal experiments. We then extend to 11 models from 6 families (\S\ref{sec:matrix}, Table~\ref{tab:models}) for the systematic analysis: adding E5-small (F), E5-small-multilingual (G), GTE-small (H), GTE-large (I), MPNet-base (J), and BGE-base (K).

\textbf{Datasets.} STSBenchmark (15K sentences), AllNLI (26K sentences), COCO Karpathy (5K image-caption pairs). We sample 2,000 anchor candidates and 500 non-overlapping queries (seed=42).

\textbf{Protocol.} FPS ($K{=}500$, based on Model~A for 5-model experiments; based on each query model for 11-model matrix) + polynomial kernel + z-score (DB-side, applied conditionally per \S\ref{sec:asymmetric}) + cosine kNN. Random baseline: Recall@1 = 0.2\%.

\subsection{Text-to-Text Retrieval}
\label{sec:text}

\begin{table}[t]
\centering
\caption{Cross-model retrieval (Recall@1 \%, AllNLI, $K{=}500$). All values from the 11-model experiment (\S\ref{sec:matrix}); representative pairs shown here.}
\label{tab:text}
\begin{tabular}{@{}llcc@{}}
\toprule
Pair & Dimensions & Baseline & Best \\
\midrule
A$\times$C (MiniLM $\times$ BGE-small)  & 384 $\times$ 384   & 97.0 & 97.2 \\
A$\times$E (MiniLM $\times$ BGE-large)  & 384 $\times$ 1024  & 80.4 & 82.4 \\
A$\times$B (MiniLM $\times$ E5-large)   & 384 $\times$ 1024  & 79.8 & 79.8 \\
C$\times$E (BGE-small $\times$ BGE-large) & 384 $\times$ 1024 & 65.6 & 80.8 \\
B$\times$C (E5-large $\times$ BGE-small) & 1024 $\times$ 384  & 24.2 & 72.8 \\
B$\times$E (E5-large $\times$ BGE-large) & 1024 $\times$ 1024 & 10.0 & 50.4 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:text} shows representative pairs from the 11-model experiment (full matrix in \S\ref{sec:matrix} and Figure~\ref{fig:heatmap}). The ``Best'' column selects between baseline and z-score normalization based on the adaptive criterion (\S\ref{sec:method}). The effect of z-score is most dramatic on pairs involving compressed models: B$\times$C improves from 24.2\% to 72.8\% (+48.6pt), while A$\times$B shows negligible change (79.8\%). Low-compression pairs (A$\times$C) achieve $>$97\% without z-score.

\textbf{Ablation.} On a separate STSBenchmark experiment with A$\times$B ($K{=}500$): Moschella baseline (random anchors + cosine) achieves 42.8\%; adding FPS raises this to 71.0\% (+28.2pt); polynomial kernel adds +12.0pt (83.0\%); z-score has negligible effect ($-$0.2pt). On B$\times$C, z-score is transformative: +57.6pt (19.0\%$\to$76.6\%).\footnote{Ablation numbers come from a separate STSBenchmark run with a different candidate pool. Cross-run variation of $\pm$3--5 points reflects FPS seed sensitivity (Appendix~\ref{app:seed}).}

\subsection{Anchor Efficiency: Scaling Curves}
\label{sec:scaling}

Figure~\ref{fig:scaling} compares the na\"ive baseline (random anchors + cosine) against the full RAT protocol across $K \in \{100, 200, 500, 1000\}$ on A$\times$B. \textbf{RAT at $K{=}100$ (55.0\%) surpasses random at $K{=}1000$ (48.2\%)}---proper anchor selection and kernel choice achieve higher accuracy with 10$\times$ fewer anchors. The FPS advantage is +29 to +36 points across all $K$ values. RAT saturates at $K{=}500$ (79.6\%), identifying it as the cost-optimal operating point.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/scaling_comparison.png}
  \caption{Scaling curves for A$\times$B: Random+cosine (dashed) vs.\ RAT protocol (solid). Left: Recall@1. Right: MRR. RAT at $K{=}100$ exceeds random at $K{=}1000$.}
  \label{fig:scaling}
\end{figure}

\subsection{Cross-Modal Retrieval}
\label{sec:crossmodal}

\begin{table}[t]
\centering
\caption{Cross-modal retrieval: MiniLM (text) $\to$ CLIP ViT-B/32 (image), Recall@1 (\%). Baseline uses polynomial kernel without z-score. $K{=}3000$ is optimal; $K{=}4000$ degrades due to the curse of dimensionality in $K$-dimensional RAT space (\S\ref{sec:limitations}). CLIP Direct (native text-image cosine) achieves 59.2\% on this query set.}
\label{tab:crossmodal}
\begin{tabular}{@{}rccc@{}}
\toprule
$K$ & Baseline & z-score & $\times$ Random \\
\midrule
500   & 16.8 & 15.6 & 84$\times$ \\
1000  & 18.6 & 16.4 & 93$\times$ \\
2000  & 20.0 & 18.0 & 100$\times$ \\
3000  & \textbf{21.2} & 18.4 & 106$\times$ \\
4000  & 19.4 & --- & 97$\times$ \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:crossmodal} shows cross-modal retrieval using Rosetta Stone anchors from COCO. A text-only encoder (MiniLM) searches a vision encoder's space (CLIP ViT-B/32) at Recall@1 = 21.2\% with $K{=}3000$ anchors---106$\times$ the random baseline and 36\% of CLIP's directly trained accuracy (59.2\%). Two findings diverge from the text-only setting: (1)~z-score normalization \emph{does not help} cross-modal retrieval (baseline consistently outperforms z-score), and (2)~performance peaks at $K{=}3000$ and declines at $K{=}4000$, indicating a dimensionality ceiling absent in text-only experiments. We analyze both phenomena in \S\ref{sec:crossmodal_analysis}.

\subsection{Scaling to 11 Text Models}
\label{sec:matrix}

To understand what determines RAT accuracy across model pairs, we extend the evaluation from 5 to 11 models spanning 6 families (Table~\ref{tab:models}), covering contrastive distillation (MiniLM), weakly-supervised (E5), contrastive+RetroMAE (BGE), multi-stage contrastive (GTE), MSE distillation (MPNet), and CLIP text. Model sizes range from 22M to 560M parameters. A 12th model (Nomic-embed-text-v1.5, L) was initially excluded due to extreme similarity compression in relative space (0.997 pairwise cosine). Subsequent analysis (\S\ref{sec:newgen}) reveals a deeper reason: L belongs to a representationally distinct cluster, sharing zero RDM correlation with models A--K. The remaining 11 BERT-family models yield 110 directed pairs for the core analysis.

\begin{table}[t]
\centering
\caption{Embedding models in the 11-model diversity experiment. sim\_mean: mean pairwise cosine similarity among 500 query embeddings---a measure of similarity compression.}
\label{tab:models}
\small
\begin{tabular}{clcccc}
\toprule
ID & Family & Params & Dim & Training & sim\_mean \\
\midrule
A & MiniLM & 22M & 384 & Contr.+Distill & 0.028 \\
B & E5 & 560M & 1024 & WeakSup+Distill & 0.716 \\
C & BGE & 33M & 384 & Contr.+RetroMAE & 0.402 \\
D & CLIP & 63M & 512 & CLIP Contr. & 0.588 \\
E & BGE & 335M & 1024 & Contr.+RetroMAE & 0.528 \\
F & E5 & 33M & 384 & WeakSup+Distill & 0.705 \\
G & E5 & 118M & 384 & WeakSup+Distill & 0.734 \\
H & GTE & 33M & 384 & Multi-stage & 0.701 \\
I & GTE & 335M & 1024 & Multi-stage & 0.690 \\
J & MPNet & 109M & 768 & MSE Distill & 0.028 \\
K & BGE & 109M & 768 & Contr.+RetroMAE & 0.408 \\
\bottomrule
\end{tabular}
\end{table}

Figure~\ref{fig:heatmap} shows the full 11$\times$11 Recall@1 matrix. Three patterns are immediately visible: (1)~CLIP text (row/column~D) is consistently weak (13--34\%), forming a distinct low-accuracy band; (2)~within-family pairs (e.g., BGE$\times$BGE, GTE$\times$GTE) cluster near the top (89--99\%); and (3)~a large block of text-only model pairs achieves 60--99\%, with significant variation.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.85\textwidth]{figures/d2a_heatmap.png}
  \caption{RAT Recall@1 (\%) for all 110 directed model pairs (best of baseline and z-score DB normalization). Rows = query model, columns = DB model.}
  \label{fig:heatmap}
\end{figure}

%% ============================================================
\subsection{Beyond BERT-Family: Scale and Cluster Structure}
\label{sec:newgen}
%% ============================================================

The 11-model evaluation above uses exclusively BERT-family encoders. To test whether RAT generalizes beyond this architectural paradigm, we expand to 17 models by adding six from recent generations (Table~\ref{tab:newgen_models}): Nomic-embed-text-v1.5 (L), GTE-Qwen2-1.5B-instruct (M, a \emph{decoder-based} LLM with 1536d output), and the Snowflake Arctic series spanning four sizes (N: 109M/768d, O: 22M/384d, P: 33M/384d, Q: 335M/1024d). The Arctic series shares a single training recipe (contrastive + RetroMAE) across a 15$\times$ parameter range, providing a controlled axis for studying scale effects.

\begin{table}[t]
\centering
\caption{New-generation models added in the 17-model expansion. All Arctic models share the same training recipe (contrastive + RetroMAE on web data).}
\label{tab:newgen_models}
\small
\begin{tabular}{clcccl}
\toprule
ID & Model & Params & Dim & sim\_mean & Notes \\
\midrule
L & Nomic-v1.5 & 137M & 768 & 0.997$^\dagger$ & Unsup.\ contrastive \\
M & GTE-Qwen2-1.5B & 1.5B & 1536 & 0.166 & Decoder-based LLM \\
N & Arctic-m & 109M & 768 & 0.623 & \\
O & Arctic-xs & 22M & 384 & 0.648 & \\
P & Arctic-s & 33M & 384 & 0.615 & \\
Q & Arctic-l & 335M & 1024 & 0.610 & \\
\bottomrule
\multicolumn{6}{@{}l@{}}{\footnotesize $^\dagger$Extreme compression; relative representations have pairwise cosine $>$0.99.}
\end{tabular}
\end{table}

\subsubsection{Cluster Discovery}
\label{sec:clusters}

For each model, we compute a Representational Dissimilarity Matrix (RDM) from 2,000 candidate embeddings using cosine distance, then measure the Spearman rank correlation between every pair of models' RDMs. This captures whether two models impose similar pairwise distance orderings on the same set of texts. The resulting 17$\times$17 matrix reveals a sharp block-diagonal partition (Figure~\ref{fig:rdm_heatmap}). The 11 BERT-family models (A--K) form one cluster with mean pairwise $\rho = 0.45$ (range 0.20--0.86). The six newer models (L--Q) form a second cluster with mean $\rho = 0.59$ (range 0.50--0.87). Critically, the cross-cluster correlation is $\rho \approx 0.00$ (range $-0.03$ to $+0.02$)---the two groups share \emph{zero} representational structure.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.85\textwidth]{figures/fig_rdm_heatmap.png}
  \caption{RDM Spearman correlation between 17 embedding models. A clear block-diagonal structure emerges: BERT-family encoders (A--K) and new-generation models (L--Q) share no representational structure ($\rho \approx 0$). The Arctic scale series (N--Q) clusters tightly within the new-generation block.}
  \label{fig:rdm_heatmap}
\end{figure}

This boundary affects all translation methods, not only RAT. On cross-cluster pairs at $K{=}500$, RAT achieves R@1 $\approx 0\%$ (e.g., M$\to$A: 0.6\%, N$\to$K: 0.0\%). Ridge regression and Procrustes alignment fail identically: 0.0--0.4\% on the same pairs. Within each cluster, all methods recover normally (e.g., N$\to$L: 40.8\% RAT / 94.6\% Ridge). The failure is structural, not methodological. We investigate this boundary in depth in \S\ref{sec:wall}.

\subsubsection{Scale Invariance of Representational Structure}
\label{sec:scale}

The Arctic series offers a natural experiment: does scaling model parameters change representational structure enough to bridge the cluster gap? Table~\ref{tab:scale_rdm} shows cross-cluster RDM correlations between each Arctic model and representative BERT-family models. \textbf{All values remain at $\rho \approx 0$ regardless of scale}, from 22M (Arctic-xs) to 335M (Arctic-l).

\begin{table}[t]
\centering
\caption{Cross-cluster RDM correlation: Arctic models vs.\ BERT-family representatives. A 15$\times$ parameter increase (22M$\to$335M) produces no change in cross-cluster structure.}
\label{tab:scale_rdm}
\small
\begin{tabular}{@{}lcccc|c@{}}
\toprule
 & O (xs, 22M) & P (s, 33M) & N (m, 109M) & Q (l, 335M) & range \\
\midrule
A (MiniLM)   & $+$0.006 & $+$0.001 & $-$0.007 & $-$0.006 & 0.013 \\
C (BGE-s)    & $-$0.006 & $-$0.017 & $-$0.008 & $-$0.012 & 0.011 \\
J (MPNet)    & $+$0.018 & $+$0.007 & $+$0.009 & $+$0.010 & 0.011 \\
\bottomrule
\end{tabular}
\end{table}

Within the Arctic series, RDM correlations are high and increase with scale proximity: O$\leftrightarrow$P (same dimension, $\rho = 0.78$), N$\leftrightarrow$Q ($\rho = 0.87$). RAT accuracy within the series ranges from 38\% to 94\% at $K{=}500$, following the expected sim\_mean and RDM dependencies.

\textbf{Implications.} The training recipe---not model size---determines representational topology. A 15$\times$ parameter increase within the same recipe produces no movement toward the BERT cluster. This places a bound on the Platonic Representation Hypothesis \citep{huh2024platonic}: within the 22M--335M range, scaling alone does not drive convergence across training paradigms. Whether this boundary dissolves at larger scale (e.g., $>$1B parameters) remains open.

Figure~\ref{fig:rdm_vs_r1} summarizes the relationship between representational compatibility and RAT accuracy across all 17 models: RDM correlation determines whether translation is possible at all, while sim\_mean predicts performance \emph{within} compatible clusters.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.75\textwidth]{figures/fig_rdm_vs_r1.png}
  \caption{RDM correlation vs.\ Recall@1 at $K{=}500$ across 17 models. Cross-cluster pairs (gray $\times$; $\rho \approx 0$) uniformly fail regardless of method or sim\_mean. Intra-cluster pairs show sim\_mean-dependent performance. Arctic scale series members are highlighted with squares.}
  \label{fig:rdm_vs_r1}
\end{figure}

%% ============================================================
\section{Analysis}
\label{sec:analysis}
%% ============================================================

\subsection{Similarity Compression as the Dominant Factor}
\label{sec:collapse}

The 110-pair matrix reveals that a single property of the embedding space---\emph{similarity compression}---is the dominant predictor of RAT accuracy. We define sim\_mean as the mean pairwise cosine similarity among query embeddings: models with high sim\_mean map all inputs to a narrow angular cone, compressing the similarity distribution and leaving little room for relative profiles to discriminate between inputs.

Figure~\ref{fig:compression} plots max(sim\_mean$_Q$, sim\_mean$_{DB}$)---the more compressed model in each pair---against Recall@1. Among text-only model pairs (excluding CLIP), the Spearman rank correlation is $\rho = -0.62$ ($p < 10^{-10}$). This single variable explains the broad structure of the heatmap: pairs involving low-compression models (MiniLM, MPNet; sim\_mean $\approx 0.03$) consistently achieve $>$80\%, while pairs involving high-compression models (E5, GTE; sim\_mean $> 0.69$) degrade to 37--70\% unless paired with a low-compression partner.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/d2a_sim_mean_vs_r1.png}
  \caption{Similarity compression vs.\ RAT accuracy. max(sim\_mean) of the pair predicts Recall@1 with $\rho = -0.62$ among text models (N=90, CLIP excluded). CLIP pairs (triangles) extend the trend.}
  \label{fig:compression}
\end{figure}

\textbf{Similarity collapse.} The extreme case of compression is \emph{similarity collapse}: Model~B (E5-large, sim\_mean = 0.72, range = 0.33) produces nearly flat relative profiles that destroy discriminability. Without z-score, B$\times$C achieves only 8.4\%; z-score adds +65 points (Table~\ref{tab:collapse}). Collapse is diagnosable via sim\_mean alone.

\begin{table}[t]
\centering
\caption{Normalization methods on B$\times$C (Recall@1 \%).}
\label{tab:collapse}
\begin{tabular}{@{}lccc@{}}
\toprule
Normalization & B$\times$C & A$\times$B & A$\times$C \\
\midrule
None                   & 8.4  & 80.6 & 98.0 \\
\textbf{z-score (DB)}  & \textbf{73.4} & \textbf{81.0} & \textbf{98.2} \\
Rank transform         & 52.0 & 67.2 & 97.6 \\
Top-$k$ mask ($k{=}50$)  & 50.2 & 54.0 & 83.0 \\
Softmax ($T{=}0.1$)     & 50.2 & 34.6 & 44.8 \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Family effect.} Same-family pairs (e.g., BGE$\times$BGE, GTE$\times$GTE) achieve 89--98\% Recall@1, consistently above cross-family pairs at comparable compression levels. The regression coefficient for same\_family is significant ($+$9 points, $p < 0.01$) even after controlling for sim\_mean. However, same\_family and same\_training are perfectly collinear in our model set---every same-family pair shares the same training objective and likely overlapping training data---so we cannot separate the contributions of shared architecture, training objective, and data overlap. Notably, BGE-small $\times$ GTE-small achieves 99.4\% despite being cross-family; both use BERT-base with contrastive objectives on web corpora. One possible explanation is that training data overlap drives the family effect, but we cannot verify this with current data---controlled experiments (e.g., identical data with different objectives) would be needed to disentangle architecture, objective, and data contributions.

\textbf{Dimensionality is irrelevant.} Models B and E share 1024d output but differ in architecture (XLM-R vs BERT); their direct cosine similarity averages $-0.02$, yet RAT achieves 55.2\%. Cross-dimension A$\times$B (384 $\times$ 1024d) reaches 84\%. Same-dimension B$\times$E (55\%) $<$ cross-dimension A$\times$B (84\%): what matters is compression level, not dimensional agreement. This provides one possible operationalization of the structural convergence posited by \citet{huh2024platonic}.

\subsection{When Z-score Helps and When It Hurts}
\label{sec:asymmetric}

The 5-model experiments established that DB-side z-score is beneficial (\S\ref{sec:method}). The 110-pair matrix reveals this is only half the story: z-score is \emph{conditionally} beneficial, with its effect determined by the DB-side model's compression level (Figure~\ref{fig:zscore}).

\begin{figure}[t]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/d2a_zscore_effect.png}
  \caption{Z-score normalization effect (Recall@1 change in percentage points) vs.\ DB-side sim\_mean. Low sim\_mean DB models benefit dramatically (+60--88 pp); high sim\_mean DB models are harmed ($-$40--62 pp). $\rho = -0.59$.}
  \label{fig:zscore}
\end{figure}

When the DB model has low sim\_mean (MiniLM, MPNet; $\approx$0.03), its relative profiles contain rich discriminative information in a wide range. Z-score rescales this well-spread information, making it accessible to compressed query profiles---hence dramatic improvements (e.g., F$\to$A: 1.0\%$\to$89.0\%, $\Delta$=+88pp). When the DB model has high sim\_mean (GTE, E5; $>$0.69), its profiles are themselves compressed with little discriminative information. Z-score amplifies noise alongside the weak signal, causing collapse (e.g., H$\to$I: 98.4\%$\to$36.8\%, $\Delta$=$-$62pp).

The Spearman correlation between z-score effect and DB sim\_mean is $\rho = -0.59$ ($p < 10^{-11}$). Partitioning at sim\_mean = 0.15: pairs with low-compression DB models gain $+$18 points on average from z-score; pairs with high-compression DB models lose $-$6 points. The practical implication is that z-score should be applied \emph{adaptively}: only when the DB model's sim\_mean is below a threshold, diagnosable from anchor embeddings alone.

\textbf{DB-side sufficiency.} Across all pairs, DB-only z-score exactly matches both-side z-score, while query-side normalization is consistently harmful ($-$8 to $-$10 points on well-spread models). This is because DB-side normalization leverages stable statistics from $n$ database vectors, while a single query vector's row-wise z-score distorts the relative magnitudes that carry discriminative signal.

\subsection{Cross-Modal Analysis}
\label{sec:crossmodal_analysis}

A natural expectation is that CLIP-text and CLIP-image, trained jointly via contrastive loss, should yield highly compatible RAT representations. The opposite holds: CLIP-text $\times$ CLIP-image (D$\times$E) achieves only 11.2\% Recall@1 at $K{=}3000$---\emph{half} of independently trained MiniLM $\times$ CLIP-image (A$\times$E, 21.2\%). We trace this to a structural mismatch between what contrastive alignment optimizes and what RAT requires.

RAT relies on \emph{local distance orderings}: if a text and its corresponding image produce similar patterns of similarity to a shared set of anchors, retrieval succeeds. We quantify this by computing, for each matched text-image pair, the Spearman rank correlation $\rho$ between their $K$-dimensional RAT vectors (Table~\ref{tab:anchor_rho}).

\begin{table}[t]
\centering
\caption{Anchor distance pattern preservation: per-pair Spearman $\rho$ between RAT vectors of matched text-image pairs ($K{=}3000$).}
\label{tab:anchor_rho}
\begin{tabular}{@{}lccc@{}}
\toprule
Pair & Mean $\rho$ & $\rho > 0.3$ & Recall@1 \\
\midrule
A$\times$E (MiniLM $\times$ CLIP-img) & \textbf{0.334} & 66.2\% & \textbf{21.2\%} \\
A$\times$D (MiniLM $\times$ CLIP-text) & 0.357 & 69.6\% & 24.0\%$^*$ \\
D$\times$E (CLIP-text $\times$ CLIP-img) & 0.183 & 12.2\% & 11.2\% \\
\bottomrule
\multicolumn{4}{@{}l@{}}{\footnotesize $^*$Text-only pair evaluated at $K{=}500$ (not $K{=}3000$); included for}\\
\multicolumn{4}{@{}l@{}}{\footnotesize reference but not directly comparable to cross-modal rows.}
\end{tabular}
\end{table}

CLIP's contrastive objective optimizes the dot product between matched text-image pairs relative to in-batch negatives---a \emph{global} alignment that does not constrain how individual anchor distances are ordered. Moreover, in-batch negative sampling may actively distort local neighborhoods: pushing apart semantically related but non-matched pairs to maximize contrastive discrimination can rearrange the ordering of nearby points in ways that break the distance patterns RAT relies on. The result is that CLIP-text's similarity profile to caption anchors bears little resemblance to CLIP-image's profile to the corresponding image anchors ($\rho = 0.18$; only 12\% of pairs exceed $\rho > 0.3$). By contrast, MiniLM---trained independently on text-only data---shares stronger local structure with CLIP-image ($\rho = 0.33$; 66\% of pairs exceed $\rho > 0.3$). Why independently trained encoders share more local structure than jointly trained ones is an open question; one hypothesis is that both MiniLM and CLIP-image independently organize semantically similar concepts into nearby regions (consistent with the Platonic Representation Hypothesis), whereas CLIP's contrastive loss actively reshapes the text space to maximize global discriminability at the expense of local structure.

Figure~\ref{fig:buckets} provides further evidence. Partitioning query pairs into quartiles by CLIP native text-image cosine similarity, A$\times$E Recall@1 rises from 14.4\% (bottom quartile) to 29.6\% (top quartile)---RAT partially reflects CLIP's alignment, accessing the same underlying structure through different means. D$\times$E shows no such trend (7--10\% across all quartiles), confirming it captures an independent and weaker signal.

\begin{figure}[t]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/bucket_analysis.png}
  \caption{RAT Recall@1 by CLIP native text-image similarity quartile. A$\times$E (MiniLM $\times$ CLIP-image) correlates with CLIP's alignment strength; D$\times$E (CLIP-text $\times$ CLIP-image) does not.}
  \label{fig:buckets}
\end{figure}

Notably, RAT also accesses structure that CLIP misses. Among the 125 queries in the lowest CLIP similarity quartile, 47 achieve Recall@5 via A$\times$E RAT; the reverse (CLIP-easy, RAT-hard) occurs in only 3 cases. RAT is not a degraded approximation of CLIP alignment but draws on partially distinct structure.

%% ============================================================
\section{The Cross-Cluster Wall}
\label{sec:wall}
%% ============================================================

The cluster boundary discovered in \S\ref{sec:clusters} raises a natural question: can it be breached? We conduct a systematic investigation, testing zero-shot methods (\S\ref{sec:wall_zeroshot}), structural diagnostics (\S\ref{sec:wall_diag}), and few-shot linear maps (\S\ref{sec:wall_fewshot}). The answer is unambiguous: \textbf{no current method---zero-shot or few-shot, parameter-free or learned---can translate across the cluster boundary.}

\subsection{Zero-Shot Methods Fail Uniformly}
\label{sec:wall_zeroshot}

We test five zero-shot approaches on three cross-cluster pairs (A$\to$O, C$\to$P, A$\to$N) spanning same-dimension and cross-dimension conditions, plus one intra-cluster control (A$\to$B). Table~\ref{tab:wall_zeroshot} shows the results.

\begin{table}[t]
\centering
\caption{Zero-shot cross-cluster translation: Recall@1 (\%) at best $K$. All cross-cluster results are indistinguishable from random (0.2\%). The control pair (A$\to$B, same cluster) confirms all methods function correctly.}
\label{tab:wall_zeroshot}
\small
\begin{tabular}{@{}lcccc@{}}
\toprule
 & \multicolumn{3}{c}{Cross-cluster} & Control \\
Method & A$\to$O & C$\to$P & A$\to$N & A$\to$B \\
\midrule
Procrustes ($K{=}2000$)       & 0.0 & 0.0 & 0.5 & 97.8 \\
Procrustes + PCA ($K{=}2000$) & --- & --- & 0.1 & 98.4 \\
RAT poly ($K{=}500$)          & 0.1 & 0.1 & 0.1 & 79.0 \\
RAT RBF ($K{=}500$)           & 0.1 & 0.1 & 0.1 & 79.1 \\
RAT poly ($K{=}2000$)         & 0.0 & 0.2 & 0.2 & 74.6 \\
Random baseline               & 0.2 & 0.2 & 0.2 & 0.2 \\
\bottomrule
\end{tabular}
\end{table}

Procrustes with up to 2,000 anchor pairs---where both the source and target embeddings of each anchor text are known---achieves 0\% on cross-cluster pairs while reaching 98\% on the control. Varying the number of anchors from 50 to 2,000, changing the kernel (polynomial, RBF, cosine), and applying PCA for dimension reduction all produce the same result: random-level performance. The wall is impervious to zero-shot methods regardless of anchor budget or algorithmic choice.

\subsection{No Shared Structure at Any Level}
\label{sec:wall_diag}

The zero-shot failure could stem from (a)~a global structural mismatch that masks residual local or cluster-level agreement, or (b)~a fundamental absence of shared structure at every granularity. To distinguish these, we compute two diagnostic measures on the same cross-cluster pairs.

\textbf{Mutual $k$-NN overlap.} For each text in the shared corpus ($N{=}2{,}500$), we compute its $k$ nearest neighbors in both the source and target embedding spaces and measure the fraction of shared neighbors. If local neighborhoods are preserved across spaces---even when global distance rankings are not---this overlap will exceed the random baseline of $k/(N{-}1)$.

\textbf{Cluster agreement.} We independently cluster each space using $k$-means and measure the Adjusted Rand Index (ARI) between the two clusterings. ARI = 0 indicates random agreement; ARI = 1 indicates perfect correspondence.

\begin{table}[t]
\centering
\caption{Structural diagnostics: cross-cluster pairs share no structure at any granularity. $k$-NN overlap ratio: observed overlap divided by random expectation (1.0$\times$ = random). ARI: Adjusted Rand Index of independent $k$-means clusterings.}
\label{tab:wall_diag}
\small
\begin{tabular}{@{}l cccc | cccc@{}}
\toprule
 & \multicolumn{4}{c|}{$k$-NN overlap ratio} & \multicolumn{4}{c}{ARI ($k$-means)} \\
Pair & $k{=}5$ & $k{=}10$ & $k{=}50$ & $k{=}100$ & 10 & 20 & 50 & 100 \\
\midrule
A$\to$O (cross) & 0.9 & 0.9 & 1.0 & 1.0 & .000 & $-$.001 & .000 & .000 \\
C$\to$P (cross) & 1.0 & 0.9 & 1.0 & 1.0 & .001 & .000 & .000 & $-$.001 \\
A$\to$N (cross) & 1.0 & 0.9 & 1.0 & 1.0 & .001 & .001 & .001 & .000 \\
\midrule
A$\to$B (control) & \textbf{209} & \textbf{107} & \textbf{22} & \textbf{12} & \textbf{.284} & \textbf{.301} & \textbf{.247} & \textbf{.212} \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:wall_diag} is unambiguous. Cross-cluster $k$-NN overlap is exactly 1.0$\times$ the random expectation at every neighborhood size tested---knowing a text's nearest neighbors in one space provides \emph{zero} information about its neighbors in the other. ARI is indistinguishable from zero at all cluster granularities. In contrast, the same-cluster control pair shows 209$\times$ random overlap at $k{=}5$ and ARI $>$ 0.2.

These diagnostics rule out the ``hidden local structure'' hypothesis. The two representational families share no structure at the global level (RDM $\rho \approx 0$, \S\ref{sec:clusters}), the local level ($k$-NN overlap $\approx$ random), or the cluster level (ARI $\approx 0$). Any method whose input is distance or similarity information---including Gromov-Wasserstein optimal transport \citep{alvarez2018gromov} and QAP-based blind matching \citep{schnaus2025blind}---faces the same absence of signal.

\subsection{Few-Shot Linear Maps Cannot Bridge the Gap}
\label{sec:wall_fewshot}

If zero-shot fails because there is no structure to preserve, perhaps supervised learning can \emph{create} a mapping. We test few-shot Ridge regression (unconstrained linear map, $W^* = \arg\min \|XW - Y\|^2 + \lambda\|W\|^2$) and Procrustes alignment (orthogonal constraint, $W^\top W = I$), using $n \in \{1, 2, 5, 10, 20, 50, 100, 200\}$ labeled query pairs. Training pairs are excluded from evaluation to prevent information leakage. Regularization $\lambda$ is selected by cross-validation (RidgeCV).

\begin{table}[t]
\centering
\caption{Few-shot cross-cluster translation: Recall@1 (\%) with training pairs excluded from evaluation. Cross-cluster pairs remain at random regardless of method or sample size. The control pair shows a smooth learning curve with Procrustes dominating Ridge.}
\label{tab:wall_fewshot}
\small
\begin{tabular}{@{}r cc|cc|cc|cc@{}}
\toprule
 & \multicolumn{2}{c|}{A$\to$O (cross)} & \multicolumn{2}{c|}{A$\to$N (cross)} & \multicolumn{2}{c|}{C$\to$P (cross)} & \multicolumn{2}{c}{A$\to$B (control)} \\
$n$ & Ridge & Proc & Ridge & Proc & Ridge & Proc & Ridge & Proc \\
\midrule
5    & 0.2 & 0.0 & 0.3 & 0.2 & 0.3 & 0.2 & 1.4 & 1.1 \\
10   & 0.2 & 0.1 & 0.1 & 0.1 & 0.2 & 0.4 & 3.0 & 2.9 \\
50   & 0.1 & 0.3 & 0.4 & 0.1 & 0.1 & 0.3 & 23.6 & 26.8 \\
100  & 0.1 & 0.2 & 0.3 & 0.2 & 0.2 & 0.3 & 47.4 & 63.7 \\
200  & 0.2 & 0.0 & 0.2 & 0.4 & 0.3 & 0.3 & 66.7 & \textbf{87.1} \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:wall_fewshot} shows that \textbf{even 200 labeled pairs produce zero improvement on cross-cluster translation}. Both Ridge and Procrustes remain at random ($\approx$0.2\%) across all three cross-cluster pairs and all sample sizes. The learned transformation fits the training pairs (by construction) but fails to generalize to held-out queries. Two factors make generalization impossible in this regime. First, the system is heavily underdetermined: a $384 \to 384$ linear map has $384^2 \approx 148$K free parameters, while 200 pairs provide only $200 \times 384 \approx 77$K constraints---regularization fills the gap, but only if nearby inputs should map to nearby outputs. Second, the diagnostic results (\S\ref{sec:wall_diag}) show this continuity assumption is violated: $k$-NN overlap at random level means nearby points in source space map to unrelated locations in target space. Neither adding more parameters (Ridge vs.\ Procrustes) nor more data (1 to 200 pairs) addresses this fundamental discontinuity.

\subsection{Within-Cluster: Procrustes as a Complement to RAT}
\label{sec:wall_positive}

The control pair (A$\to$B) reveals a positive finding. With 200 labeled pairs, Procrustes achieves \textbf{87.1\% R@1}---substantially above zero-shot RAT's 79.0\% on the same pair. Ridge reaches only 66.7\% under the same conditions, demonstrating that the orthogonal constraint acts as effective regularization, not a limitation, in the data-scarce regime. Comparing with the D1 comparison (\S\ref{sec:alignment}), where Procrustes achieves 79.9\% at $K{=}500$, the few-shot setup here uses query pairs rather than anchor pairs, operating in a different regime.

This establishes a practical complementarity within each cluster: \textbf{RAT provides a strong zero-shot baseline; adding even modest supervision via Procrustes yields further gains.} The training recipe determines whether translation is \emph{possible}; the available supervision determines how \emph{accurate} it can be.

%% ============================================================
\section{A Three-Layer Diagnostic Model}
\label{sec:diagnostic}
%% ============================================================

The preceding experiments reveal that embedding space translation accuracy is governed by a hierarchy of factors operating at different scales. We synthesize these findings into a three-layer diagnostic model that predicts---from anchor embeddings alone---whether translation is feasible and what accuracy to expect.

\subsection{Layer 1: RDM Correlation as Gatekeeper}
\label{sec:layer1}

RDM correlation determines whether translation is \emph{possible}. When $\rho \approx 0$, no method succeeds: not zero-shot RAT (\S\ref{sec:wall_zeroshot}), not Procrustes or Ridge with 2,000 anchor pairs, not few-shot learning with 200 labeled pairs (\S\ref{sec:wall_fewshot}), and---as the structural diagnostics show (\S\ref{sec:wall_diag})---not any method that depends on distance or neighborhood structure.

This gatekeeper is binary in practice. Among 272 directed pairs in our 17-model evaluation, every pair with RDM $\rho < 0.1$ achieves R@1 $< 1\%$ regardless of method and anchor budget. Every pair with $\rho > 0.2$ achieves R@1 $> 10\%$ with at least one method. No pair falls in between. The threshold is computable from anchor embeddings of both models on a shared text set (seconds on CPU).

\subsection{Layer 2: Similarity Compression Governs Accuracy Range}
\label{sec:layer2}

Conditional on passing the RDM gate ($\rho > 0.1$), the dominant predictor of translation accuracy is similarity compression (sim\_mean), as established in \S\ref{sec:collapse}. Among BERT-family pairs (110 directed), the Spearman correlation between max(sim\_mean) and RAT R@1 is $\rho = -0.62$ ($p < 10^{-10}$). Hierarchical regression on all intra-cluster pairs shows sim\_mean and RDM contribute comparably to explained variance (each $R^2 \approx 0.25$ individually; $R^2 = 0.57$ jointly), confirming that neither subsumes the other within compatible clusters.

Sim\_mean also determines method selection. It governs when z-score normalization helps ($\rho = -0.59$ with z-score effect, \S\ref{sec:asymmetric}), which direction of RAT is better (\S\ref{sec:asymmetry}), and the K crossover between RAT and linear methods (weak correlation with sim\_mean, $\rho = -0.46$, $p = 0.06$; no correlation with RDM, $\rho = 0.14$, $p = 0.59$).

\subsection{Layer 3: Structural Factors}
\label{sec:layer3}

After accounting for RDM gatekeeper and sim\_mean, residual variance is explained by three structural factors: (1)~same-family pairs gain $+$9 percentage points after controlling for compression (\S\ref{sec:collapse}); (2)~dimension mismatch amplifies directional asymmetry (mean $|\Delta| = 18$ pp for cross-dimension vs.\ 2.6 pp for same-dimension pairs, \S\ref{sec:asymmetry}); and (3)~within-cluster RDM provides fine-grained prediction (partial correlation $\rho = 0.80$, $p < 10^{-40}$ after controlling for sim\_mean).

\subsection{Practical Decision Flowchart}
\label{sec:flowchart}

The three-layer model yields a concrete protocol for deploying embedding translation (Figure~\ref{fig:flowchart}):

\begin{figure}[t]
\centering
\small
\fbox{\parbox{0.92\columnwidth}{%
\textbf{Embedding Translation Decision Protocol}\\[4pt]
\textbf{Input:} Models $X$, $Y$; shared anchor texts $\mathcal{A}$ ($K \ge 200$).\\[4pt]
\textbf{Step 1: Feasibility check} (Layer 1)\\
\quad Compute RDM correlation $\rho$ from anchor embeddings.\\
\quad If $\rho < 0.1$: \textsc{Translation Not Feasible}.\\
\quad\quad $\to$ Re-embed with a single model, or accept incompatibility.\\[3pt]
\textbf{Step 2: Method selection} (Layers 2--3)\\
\quad Compute sim\_mean for both models from anchors.\\
\quad If labeled pairs available ($n \ge 50$): use \textbf{Procrustes} (\S\ref{sec:wall_positive}).\\
\quad Else if $K \ge 200$ anchors: use \textbf{Ridge/Affine} (\S\ref{sec:alignment}).\\
\quad Else ($K < 50$): use \textbf{RAT} with FPS in lower sim\_mean space.\\[3pt]
\textbf{Step 3: Normalization}\\
\quad If DB-side sim\_mean $< 0.65$: apply z-score to DB relative repr.\\
\quad Else: skip z-score.
}}
\caption{Practical decision protocol derived from the three-layer diagnostic model. All inputs are computable from anchor embeddings on CPU.}
\label{fig:flowchart}
\end{figure}

All inputs---RDM, sim\_mean, anchor embeddings---are computable from black-box model access on a shared text set. No retrieval evaluation is needed to make the decision.

%% ============================================================
\section{Applications}
\label{sec:applications}
%% ============================================================

The 110-pair evaluation establishes RAT's accuracy profile. We now re-analyze this data through the lens of three practical deployment scenarios, requiring no new computation.

\subsection{Model Update Without Re-indexing}

When upgrading from an old embedding model to a new one, the standard approach requires re-encoding the entire database---a cost that scales linearly with database size. RAT replaces this with encoding only $K$ anchor texts with both models ($K{=}500$ in our experiments).

Among 7 same-family upgrade pairs (BGE-small$\to$base, small$\to$large, base$\to$large; E5 and GTE variants), RAT achieves a mean \textbf{Retention Rate} (RAT Recall@1 / re-index Recall@1) of \textbf{93.6\%}, with all pairs above 88\%. For a 1M-document database, re-indexing with a large model takes $\sim$3.5 hours; RAT requires $\sim$6 seconds (500 anchor encodes + matrix multiplication), a cost ratio of 0.05\%.

\subsection{Lightweight Query Gateway}

Using a small, fast model for query encoding while searching a database built with a larger, more accurate model reduces query latency without rebuilding the database. GTE-small (33M params, $\sim$500 docs/sec) querying a GTE-large (335M params, $\sim$80 docs/sec) database achieves \textbf{98.4\% Recall@1 with 6.2$\times$ query speedup}. Cross-family pairs (e.g., MiniLM$\to$BGE-large) achieve 82.4\% at the same speedup.

\subsection{Multi-Database Unified Search}

Organizations with databases built using different models can search them jointly via RATHub. However, naive concatenation of relative representations fails: score scales differ across models (e.g., GTE's compressed representations produce cosine similarities near 1.0, dominating results from other databases). \textbf{Per-database score normalization}---z-scoring similarity scores within each database before merging---resolves this, achieving Recall@1 = 68.8\% and \textbf{Recall@5 = 95.6\%} across three databases (BGE-large, MiniLM, GTE-small). RRF achieves only 33.4\% Recall@1 due to discarding confidence information. Full comparison of merging strategies appears in Appendix~\ref{app:multidb}.

%% ============================================================
\section{Comparison with Direct Alignment}
\label{sec:alignment}
%% ============================================================

RAT requires no learned parameters---but is this an advantage or a limitation?  When paired anchor embeddings are available, one can fit a linear transformation directly.  We systematically compare RAT against three direct alignment methods across all 132 directed model pairs, varying the number of anchor pairs from 10 to 500.

\subsection{Setup}

We compare four methods, all using the same FPS-selected anchor pairs:
\begin{itemize}
  \item \textbf{RAT}: polynomial kernel + adaptive z-score (our protocol).
  \item \textbf{Orthogonal Procrustes}: $W^* = \arg\min \|AW - B\|^2$ s.t.\ $W^\top W = I$. Restricted to same-dimension pairs (32 of 132).
  \item \textbf{Ridge Regression}: $W^* = (A^\top A + \lambda I)^{-1} A^\top B$, with $\lambda$ selected per pair from $\{10^{-4}, 10^{-2}, 1, 100\}$ by Recall@1 on the test set.  This oracle $\lambda$ selection represents an \emph{upper bound} on Ridge performance; in practice, $\lambda$ selection requires a held-out validation set, and the optimal $\lambda$ shifts with $K$ (e.g., $10^{-4}$ at $K{=}50$ vs.\ $1.0$ at $K{=}500$), adding tuning overhead that RAT avoids entirely.
  \item \textbf{Affine}: Ridge with a bias term.
\end{itemize}
All methods use anchor pairs for fitting (or RAT encoding) and are evaluated on 500 held-out queries.  Anchor counts $K \in \{10, 25, 50, 100, 200, 500\}$ span the range from extremely data-scarce to abundant.  Three FPS seeds confirm stability (mean std $< 0.6$ percentage points).

\subsection{Main Results: The Crossover at $K \approx 100$}

Table~\ref{tab:d1_main} and Figure~\ref{fig:d1_r1_vs_k} reveal a clear crossover: RAT dominates at low $K$ and linear methods dominate at high $K$.

\begin{table}[t]
\centering
\caption{Mean Recall@1 (\%) across model pairs by anchor count $K$. RAT and Ridge/Affine: $N{=}132$ directed pairs. Procrustes: $N{=}32$ (same-dimension only). RAT$_\text{auto}$: $N{=}66$ undirected pairs, each selecting the better of two directions (oracle; see \S\ref{sec:asymmetry}). Ridge/Affine $\lambda$ selected per pair by test-set Recall@1 (oracle upper bound).}
\label{tab:d1_main}
\small
\begin{tabular}{@{}lrcccccc@{}}
\toprule
Method & $N$ & $K{=}10$ & $K{=}25$ & $K{=}50$ & $K{=}100$ & $K{=}200$ & $K{=}500$ \\
\midrule
RAT                 & 132 & \textbf{5.9}  & \textbf{19.0} & \textbf{30.4} & 37.5 & 42.7 & 46.0 \\
RAT$_\text{auto}$   &  66 & \textbf{7.4}  & \textbf{22.7} & \textbf{38.7} & \textbf{51.2} & 58.5 & 61.8 \\
Procrustes          &  32 & 2.1  & 7.8  & 23.0 & 51.1 & 70.6 & 79.9 \\
Ridge               & 132 & 2.2  & 8.5  & 23.2 & 45.6 & 62.4 & 72.9 \\
Affine              & 132 & 1.8  & 7.5  & 22.2 & 45.8 & 63.2 & 73.1 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
  \centering
  \includegraphics[width=0.75\textwidth]{figures/d1_r1_vs_k.png}
  \caption{Recall@1 vs.\ number of anchors $K$. RAT dominates at $K \le 50$; linear methods overtake at $K \ge 200$. The crossover zone ($K \approx 50$--100) is shaded.  Error bars: $\pm 1$ std over 3 FPS seeds.}
  \label{fig:d1_r1_vs_k}
\end{figure}

At $K{=}50$, RAT$_\text{auto}$ wins 74\% of pairs (mean $\Delta{=}{+}12.0$ pp over the best linear method).  At $K{=}500$, the relationship reverses: Ridge/Affine achieve 73\% mean Recall@1 vs.\ RAT's 46\%, winning 90\% of pairs.  The crossover at $K \approx 100$ marks where the linear methods accumulate enough training samples to outperform RAT's parameter-free approach.

The asymmetry is structural: RAT projects into a $K$-dimensional space where $K$ is both the representation dimensionality \emph{and} the information budget; linear methods project into the target model's $d$-dimensional space, using $K$ anchors only as training samples.  At low $K$, the linear methods are underdetermined ($K < d$), while RAT's $K$-dimensional representation is well-conditioned by construction.  At high $K$, the linear methods converge toward a near-optimal mapping, while RAT's representation accumulates redundant anchors that dilute discriminability (confirmed by scaling experiments showing performance \emph{declining} at $K{=}2000$ for representative pairs).

\textbf{Hyperparameter dependence.}  Ridge and Affine require selecting $\lambda$, whose optimal value shifts with $K$ (e.g., $\lambda{=}10^{-4}$ at $K{=}50$ vs.\ $\lambda{=}1.0$ at $K{=}500$).  RAT has no such hyperparameter.

\textbf{Dimensionality constraint.}  Procrustes requires $d_X = d_Y$, limiting it to 32 of 132 pairs.  Ridge/Affine handle cross-dimension pairs but are particularly underdetermined when $K \ll d$ (e.g., $K{=}50$ anchors for a $384{\to}1024$ mapping).  RAT handles all pairs uniformly.

\subsection{Directional Asymmetry}
\label{sec:asymmetry}

RAT exhibits pronounced directional asymmetry: the same model pair can yield vastly different accuracy depending on which model's space is used for FPS anchor selection (mean asymmetry = 31.6 pp, max = 95.0 pp at $K{=}500$).  Linear methods show much smaller asymmetry (Ridge: 6.4 pp, Affine: 6.1 pp), because the learned transformation is approximately direction-agnostic.

\begin{figure}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/d1_simmean_scatter.png}
  \caption{Directional asymmetry is driven by similarity compression.  Each undirected pair contributes two points: the better direction (blue) and worse direction (red).  Points in the harmful zone (sim\_mean $\ge 0.65$, right of dashed line) cluster near 0\% Recall@1.}
  \label{fig:d1_scatter}
\end{figure}

Figure~\ref{fig:d1_scatter} reveals the cause: when FPS runs in a compressed space (sim\_mean $\ge 0.65$), anchor selection degenerates---points are tightly clustered, FPS cannot achieve coverage, and relative profiles become uninformative.  The Spearman correlation between $|\Delta\text{sim\_mean}|$ and asymmetry is $\rho = 0.47$ ($p < 10^{-4}$).

This motivates \textbf{bidirectional evaluation}: run FPS in both model spaces, compute RAT retrieval in both directions, and select the direction with higher accuracy.  Since FPS and RAT encoding run in milliseconds, the computational overhead is negligible.  We denote this oracle selection \textbf{RAT$_\text{auto}$} in Table~\ref{tab:d1_main}.\footnote{RAT$_\text{auto}$ uses oracle direction selection (best of two directions on the test set).  In deployment without a test set, selecting the lower sim\_mean space as the FPS space correctly predicts the better direction 74\% of the time.  A practical deployment would compute RAT retrieval in both directions and select the one with higher mean similarity score, which closely approximates oracle selection.}  RAT$_\text{auto}$ improves mean Recall@1 from 46.0\% to 61.8\% at $K{=}500$ (+15.8 pp), narrowing the gap with linear methods from 27.1 pp to 11.3 pp.

\subsection{Same-Dimension vs.\ Cross-Dimension Pairs}

When dimensions match, linear methods have a structural advantage: Procrustes finds an exact orthogonal mapping within the shared dimensionality.  Among same-dimension pairs ($N{=}16$ undirected), RAT$_\text{auto}$ wins 81\% at $K{=}50$ but 0\% at $K{=}500$.

Among cross-dimension pairs ($N{=}50$ undirected), where Procrustes is inapplicable, RAT$_\text{auto}$ wins 72\% at $K{=}50$ (mean $\Delta{=}{+}12.0$ pp over Ridge/Affine) and retains a 12\% win rate even at $K{=}500$.  This is RAT's strongest structural advantage: it handles arbitrary dimension mismatches without modification, while Ridge/Affine must fit a rectangular $d_X \times d_Y$ mapping that is severely underdetermined at low $K$.

\subsection{Practical Guidelines}

The comparison yields concrete deployment advice:
\begin{itemize}
  \item $K \le 50$ anchors: \textbf{use RAT}.  Linear methods are underdetermined and unstable.
  \item $K \ge 200$ anchors: \textbf{use Ridge/Affine}.  Sufficient training data enables near-optimal learned mappings.  Use RAT only if dimensions differ and no hyperparameter tuning is feasible.
  \item $50 < K < 200$: either method is viable; RAT$_\text{auto}$ is competitive and parameter-free.
  \item \textbf{Always}: run FPS in the lower sim\_mean space, or use bidirectional evaluation.
\end{itemize}

%% ============================================================
\section{Related Work}
\label{sec:related}
%% ============================================================

\textbf{Relative Representations.} \citet{moschella2023relative} introduced the framework of encoding data points as similarity vectors to shared anchors, demonstrating zero-shot model stitching. \citet{garcia2024geometric} propose geometric improvements---anisotropic scaling invariance and topological densification---that improve zero-shot stitching on NLP tasks.  RAT operates within this framework: the core idea is theirs. Our contributions are (1)~showing that anchor selection matters critically (FPS achieves with 100 anchors what random requires 1,000), (2)~identifying similarity collapse as a previously uncharacterized failure mode and resolving it with adaptive z-score (a normalization approach complementary to the geometric corrections of \citealt{garcia2024geometric}), and (3)~extending to cross-modal retrieval via paired anchors.

\textbf{Platonic Representation Hypothesis.} \citet{huh2024platonic} hypothesize that models trained on sufficient data converge toward shared representations. Our finding that models with zero direct correlation share relative structure (B$\times$E: 55\%) provides empirical support within the BERT-family cluster. However, extending to 17 models (\S\ref{sec:newgen}) reveals a cluster boundary (RDM $\rho \approx 0$) that suggests convergence occurs \emph{within} training paradigms but not yet across them, even at 335M parameters (\S\ref{sec:scale}). RAT's cross-model Recall@1 offers a quantitative operationalization of convergence degree, computable from black-box model access alone.

\textbf{Model Stitching and Alignment.} \citet{bansal2021revisiting} train affine stitching layers; \citet{merullo2022linearly} learn linear projections for vision-language bridging; Procrustes alignment \citep{conneau2018word} fits orthogonal maps. \citet{lahner2024direct} systematically compare anchor-based relative representations against direct linear/affine alignment, finding that linear transformations often suffice with enough correspondences. Our comparison (\S\ref{sec:alignment}) extends this analysis to 132 directed pairs across 11 models, identifying a crossover at $K \approx 100$ anchors and characterizing the conditions under which each approach excels.

\textbf{Unsupervised Alignment and Structural Matching.} Gromov-Wasserstein optimal transport \citep{alvarez2018gromov} aligns spaces without parallel data by matching intra-domain distance matrices. \citet{schnaus2025blind} achieve blind matching of vision-language embeddings via QAP on pairwise distances, demonstrating that structural matching can succeed when representations share local distance patterns. Our diagnostic results (\S\ref{sec:wall_diag}) show that these approaches face the same limitation as RAT when the underlying distance structures are uncorrelated: mutual $k$-NN overlap at random level implies no signal for any distance-based method.

%% ============================================================
\section{Limitations}
\label{sec:limitations}
%% ============================================================

\textbf{Evaluation scale.} All experiments use 500 queries from English-language datasets. Evaluation on larger benchmarks (e.g., MTEB) and multilingual settings is needed. \textbf{Cross-modal ceiling.} Recall@1 of 21.2\% is a proof of concept, not a practical system; the gap to CLIP's native accuracy (59\%) indicates fundamental limits of zero-shot cross-modal translation. \textbf{RAT scaling ceiling.} RAT performance peaks at $K \approx 200$--500 for text-to-text and degrades beyond: on representative pairs, $K{=}2000$ reduces Recall@1 by 1.6--4.4 pp compared to $K{=}500$.  In contrast, Ridge continues improving monotonically with $K$.  The likely mechanism is noise accumulation in high-$K$ cosine similarity, where redundant anchors dilute the discriminative signal.  For cross-modal retrieval, this effect is more pronounced: at $K{=}3000$, matched text-image pairs have mean RAT cosine similarity of 0.983 vs.\ 0.981 for unmatched pairs---a margin of only 0.002, explaining the peak at $K{=}3000$ and decline at $K{=}4000$ (Table~\ref{tab:crossmodal}). \textbf{Polynomial kernel DC bias.} The kernel $\kappa(u,v) = (u^\top v + 1)^2$ introduces a large constant offset when sim\_mean is high: at sim\_mean = 0.5, the mean output is $(0.5 + 1)^2 = 2.25$ with typical variation of $\pm$0.15, yielding a signal-to-offset ratio of 15:1. One model (Nomic-embed-text-v1.5) produced pairwise cosine similarity of 0.997 in relative representation space, making retrieval impossible. Centering the kernel or applying per-vector z-score resolves this, but the current protocol does not detect or correct it automatically. \textbf{Compression--family confound.} Same\_family and same\_training are perfectly collinear in our 11-model set; separating the contributions of shared training data, architecture, and objective requires controlled experiments (e.g., same data with different objectives) that our model selection does not permit. \textbf{Representational cluster boundary.} Extending to 17 models reveals that BERT-family encoders and newer models (Nomic, GTE-Qwen2, Arctic) share zero RDM correlation at every structural level (\S\ref{sec:wall}). Neither zero-shot methods, few-shot linear maps (up to 200 labeled pairs), nor structural matching approaches can bridge this gap. Scaling parameters 15$\times$ within a training recipe does not move cross-cluster RDM (\S\ref{sec:scale}). Whether this boundary dissolves at much larger scale ($>$1B parameters with diverse training) or requires fundamentally different approaches (e.g., nonlinear maps with large paired datasets) remains open. \textbf{Few-shot evaluation.} Our few-shot experiments use up to 200 labeled pairs; we have not tested regimes with thousands of pairs, where nonlinear methods might succeed. \textbf{Theory.} We provide no formal proof of why relative profiles align; sufficient conditions remain open.

%% ============================================================
\section{Conclusion}
\label{sec:conclusion}
%% ============================================================

RAT translates between embedding spaces without training via three composable steps (FPS + polynomial kernel + adaptive DB-side z-score), achieving Recall@1 from 13\% to 99\% across 110 directed pairs within compatible model families.

Evaluating 17 models from 9 families---including BERT-family encoders, a decoder-based LLM, and a scale series spanning 22M to 335M parameters---we identify a three-layer hierarchy governing embedding translation. First, RDM correlation acts as a binary gatekeeper: models from different training paradigms share zero representational structure at every granularity (global distance rankings, local neighborhoods, cluster assignments), and no method---zero-shot, few-shot, or structural matching---can bridge this gap. This boundary is invariant to 15$\times$ parameter scaling within a training recipe. Second, within compatible clusters, similarity compression ($\rho = -0.62$) predicts accuracy and determines both normalization strategy and method selection. Third, family membership and dimensionality refine predictions.

This hierarchy yields a practical decision protocol: compute RDM correlation and sim\_mean from anchor embeddings to determine feasibility, select the optimal method (RAT for $K \le 50$; Ridge for $K \ge 200$; Procrustes with labeled pairs), and predict accuracy---all without retrieval evaluation. Three application scenarios demonstrate practical value: model upgrades retain 94\% accuracy at 0.05\% of re-indexing cost; lightweight query gateways achieve 98\% accuracy with 6$\times$ speedup; and multi-database unified search reaches Recall@5 = 96\%. The protocol is available as an open-source library (\texttt{pip install rat-embed}).

%% ============================================================
% References
%% ============================================================

\bibliographystyle{plainnat}

\begin{thebibliography}{10}

\bibitem[Bansal et al.(2021)]{bansal2021revisiting}
Yamini Bansal, Preetum Nakkiran, and Boaz Barak.
\newblock Revisiting model stitching to compare neural representations.
\newblock In \emph{NeurIPS}, 2021.

\bibitem[Conneau et al.(2018)]{conneau2018word}
Alexis Conneau, Guillaume Lample, Marc'Aurelio Ranzato, Ludovic Denoyer, and Herv{\'e} J{\'e}gou.
\newblock Word translation without parallel data.
\newblock In \emph{ICLR}, 2018.

\bibitem[Garc{\'\i}a-Castellanos et al.(2024)]{garcia2024geometric}
Adri{\'a}n Garc{\'\i}a-Castellanos, Guillem Ramirez-Miranda, and Jordi Armengol-Estap{\'e}.
\newblock Towards a geometric understanding of relative representations.
\newblock In \emph{UniReps Workshop at NeurIPS}, 2024.

\bibitem[Hinton et al.(2015)]{hinton2015distilling}
Geoffrey Hinton, Oriol Vinyals, and Jeff Dean.
\newblock Distilling the knowledge in a neural network.
\newblock \emph{arXiv preprint arXiv:1503.02531}, 2015.

\bibitem[Huh et al.(2024)]{huh2024platonic}
Minyoung Huh, Brian Cheung, Tongzhou Wang, and Phillip Isola.
\newblock The platonic representation hypothesis.
\newblock In \emph{ICML}, 2024.

\bibitem[Karpathy \& Fei-Fei(2015)]{karpathy2015deep}
Andrej Karpathy and Li Fei-Fei.
\newblock Deep visual-semantic alignments for generating image descriptions.
\newblock In \emph{CVPR}, 2015.

\bibitem[Lin et al.(2014)]{lin2014microsoft}
Tsung-Yi Lin, Michael Maire, Serge Belongie, James Hays, Pietro Perona, Deva Ramanan, Piotr Doll{\'a}r, and C.~Lawrence Zitnick.
\newblock Microsoft {COCO}: Common objects in context.
\newblock In \emph{ECCV}, 2014.

\bibitem[Merullo et al.(2022)]{merullo2022linearly}
Jack Merullo, Louis Castricato, Carsten Eickhoff, and Ellie Pavlick.
\newblock Linearly mapping from image to text space.
\newblock In \emph{ICLR}, 2022.

\bibitem[L\"ahner \& Moeller(2024)]{lahner2024direct}
Zorah L\"ahner and Michael Moeller.
\newblock On the direct alignment of latent spaces.
\newblock In \emph{AISTATS (PMLR)}, 2024.

\bibitem[Moschella et al.(2023)]{moschella2023relative}
Luca Moschella, Valentino Maiorca, Marco Fumero, Antonio Norelli, Francesco Locatello, and Emanuele Rodol{\`a}.
\newblock Relative representations enable zero-shot latent space communication.
\newblock In \emph{ICLR}, 2023.

\bibitem[Alvarez-Melis \& Jaakkola(2018)]{alvarez2018gromov}
David Alvarez-Melis and Tommi~S. Jaakkola.
\newblock Gromov-{W}asserstein alignment of word embedding spaces.
\newblock In \emph{EMNLP}, 2018.

\bibitem[Schnaus et al.(2025)]{schnaus2025blind}
Felix Schnaus, Katharina Prasse, and Sjoerd van Steenkiste.
\newblock It's a ({B}lind) {M}atch! {B}lind embedding matching of vision and language representations.
\newblock In \emph{CVPR}, 2025.

\end{thebibliography}

%% ============================================================
% Appendix
%% ============================================================

\appendix

\section{Kernel Comparison}
\label{app:kernels}

\begin{table}[h]
\centering
\caption{Kernel function comparison on A$\times$B (Recall@1 \%, FPS $K{=}500$).}
\label{tab:kernels}
\begin{tabular}{@{}lc@{}}
\toprule
Kernel & Recall@1 \\
\midrule
Cosine ($u^\top v$) & 66.4 \\
Polynomial $(u^\top v + 1)^2$ & \textbf{77.2} \\
RBF ($\exp(-\gamma \|u-v\|^2)$) & 61.8 \\
\bottomrule
\end{tabular}
\end{table}

\section{FPS Seed Sensitivity}
\label{app:seed}

FPS selects a random initial point before greedily maximizing coverage. Table~\ref{tab:seed} evaluates 5 random seeds on 5 representative pairs spanning the accuracy range. The mean standard deviation across all pairs and conditions is \textbf{1.0 percentage point}; the maximum is 3.7pp (E$\to$I with z-score). Cross-table variation of $\pm$3pt in the main text is within this expected range.

\begin{table}[h]
\centering
\caption{FPS seed sensitivity: Recall@1 (\%) across 5 random seeds, $K{=}500$.}
\label{tab:seed}
\small
\begin{tabular}{@{}lccccc|cc@{}}
\toprule
 & \multicolumn{5}{c|}{Seeds} & \multicolumn{2}{c}{Summary} \\
Pair & 42 & 123 & 456 & 789 & 2024 & Mean & Std \\
\midrule
A$\to$C (base)  & 97.0 & 97.2 & 96.6 & 97.2 & 96.8 & 97.0 & 0.2 \\
A$\to$B (base)  & 79.8 & 78.4 & 82.0 & 79.2 & 81.0 & 80.1 & 1.3 \\
B$\to$C (z-sc)  & 72.8 & 76.0 & 73.0 & 75.2 & 71.8 & 73.8 & 1.6 \\
E$\to$I (base)  & 98.8 & 98.8 & 98.8 & 98.6 & 98.6 & 98.7 & 0.1 \\
D$\to$J (z-sc)  & 22.0 & 20.8 & 21.0 & 19.8 & 20.6 & 20.8 & 0.7 \\
\bottomrule
\end{tabular}
\end{table}

\section{Multi-Database Merging Strategies}
\label{app:multidb}

Table~\ref{tab:multidb} compares three strategies for merging search results across databases built with different embedding models (BGE-large, MiniLM, GTE-small; 167 documents each; BGE-large queries).

\begin{table}[h]
\centering
\caption{Multi-database merging strategies. Per-DB score normalization (z-score on similarity scores per database before merging) achieves the best Recall@1.}
\label{tab:multidb}
\begin{tabular}{@{}lccc@{}}
\toprule
Method & R@1 & R@5 & R@10 \\
\midrule
Baseline (re-index all) & 100.0 & 100.0 & 100.0 \\
\textbf{Score normalization} & \textbf{68.8} & \textbf{95.6} & \textbf{99.2} \\
Reciprocal Rank Fusion & 33.4 & 96.8 & 98.4 \\
Naive vstack & 30.8 & 33.2 & 33.2 \\
\bottomrule
\end{tabular}
\end{table}

Naive vstack fails because relative representations from different models have incompatible score scales: GTE-small (sim\_mean = 0.68, z-score skipped) produces norms 3$\times$ higher than other models, causing its documents to dominate all rankings. Score normalization resolves this by standardizing per-query similarity distributions within each database before merging. RRF discards confidence information, treating a DB1 top-1 with score 0.95 identically to a DB2 top-1 with score 0.3.

\section{Reproducibility}
\label{app:repro}

All experiments run on CPU (Intel, WSL2). Software: Python 3.12, sentence-transformers $\geq$2.2, transformers $\geq$4.30, scikit-learn $\geq$1.3. Random seed = 42 for all experiments unless otherwise noted. Code: \url{https://github.com/jiro-prog/rat-experiment}. Library: \texttt{pip install rat-embed} (v0.3.0).

\end{document}
